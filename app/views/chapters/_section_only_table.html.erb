<tr>
  <td rowspan="<%= rowspan(section.id) %>" class="category">
    <%= @chapter.number%>.<%= section.number%><br/>
    <%= section.name %>
  </td>
  <td colspan="6" class="category"><%= link_to '要求を追加', new_requirement_path(section_id: section.id) %></td>
</tr>
