<%= form_with(model: permission, local: true) do |form| %>
  <% if permission.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(permission.errors.count, "error") %> prohibited this permission from being saved:</h2>

      <ul>
      <% permission.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :role_id %>
    <%= form.number_field :role_id, id: :permission_role_id %>
  </div>

  <div class="field">
    <%= form.label :operation_id %>
    <%= form.number_field :operation_id, id: :permission_operation_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
